1. Execute the create database command that is commented out in ProjectTimeManager.sql (CREATE DATABASE ProjectTimeManager) in the (localdb)\MSSQLLocalDb SQL Server Instance.
	1.1 To change the SQL Server Instance you must modify the 'SqlConnection' connection string in the web.config in the WebApp project to point to the correct SQL Server Instance.
2. Execute the create table scripts in the ProjectTimeManager.sql file
3. Populate the Employee and Project tables in the database using Employee_DATA.sql and Project_DATA.sql

